export interface AgtkCommandObjectFilterEffectRemoveConfig {
  /**
   * (1 << value) is assigned to value (value) specified from Agtk.constants.filterEffects.
   * Use OR operators to specify multiple.
   */
  removeBit: number;
  duration300: number;
}
